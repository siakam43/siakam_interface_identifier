# 原始需求

## 整体说明
1. 我需要开发一个单纯由markdown文档组成的skill, 名字叫siakam_interface_identifier
2. 核心功能: 识别出目标项目中，哪些函数是“外部接口”。“外部接口”定义：本项目模块定义的函数，作用是提供给外部用户或外部模块与本项目模块进行通信交互。我们更关心向本项目模块传入外部（不可信）数据的外部接口。

## 流程与架构
### 0 工具输入
1. SKILL使用方法：/siakam_interface_identifier  project_dir 
2. 参数project_dir为待分析项目路径，如果用户没有指定该参数，默认为当前文件夹。
3. 在project_dir中，如果存在.siakamignore文件，需要将该文件中指定的子目录或文件排除掉，不进行分析。.siakamignore文件语法和.gitignore语法一样。
4. 在project_dir中，应该存在.siakam_out文件夹，内部应该存在entry.json文件。如果发现该文件不存在，则提示用户提供该文件再继续。

### 步骤1：了解项目架构
1. 该步骤主要功能是了解待分析项目的架构设计。了解本项目模块的功能，由哪些子模块组成，推测哪些子模块需要和外部模块通信。进而推测出（仅能分析本项目模块代码，没有外部模块代码提供分析）本项目模块可能与哪些外部模块交互。
2. 根据project_dir和.siakamignore文件，识别需要分析的代码范围。
3. 利用LLM的代码分析能力来实现项目分析任务，分析结果保存在.siakam_out/arch.md

### 步骤2：解析entry.json文件
1. entry.json中包含了该项目中可能为外部接口的函数信息，这些函数都具备没有被任何项目内部函数调用的特点。这些函数将作为目标函数待后续分析。
2. entry.json的数据格式为：
'''json
{
  "entry_functions": [
    {
      "name": "FUNC_NAME",
      "file": "FILE_PATH",
      "line_start": LINE_NUM
    },
    {
      ...
    }
  ]
}
'''

### 步骤3：分析目标函数
1. 该步骤是本skill的核心，逐个分析目标函数是否为“外部接口”
2. 目标函数虽然没有被任何模块内部函数调用，但存在误报情况，满足如下条件的函数不应该是“外部接口”
- 目标函数被间接调用，没有被识别出来
- 目标函数为测试函数
- 目标函数为冗余函数
3. skill需要分析目标函数的功能，结合步骤1的架构分析结果，推理出是否为“外部接口”，如果是，该接口在与哪个外部模块进行通信。并给出置信度评分。
4. 本步骤由LLM完成分析，应该由markdown文档组成（此处的提示词极其重要，关系到分析准确率，待详细设计）。在Agent(claude/opencode)调用skill时，会使用LLM完成分析任务。重要！本模块禁止使用任何脚本和代码！
5. 目标函数数量很大，但分析相护独立。提示词应该要求采取多subagent并行分析的策略，同时应有任务列表文件（临时文件，全部任务结束后清理掉，可以暂存在.siakam_out目录）来跟踪进度，避免任务丢失。最终汇总结果，保存在.siakam_out/apis.json。文件中应包含确认为“外部接口”的函数信息，包含函数名，位置，置信度，分析详情等等，格式待设计。
6. LLM应尽最大努力，精准分析每一个目标函数详情，做出最合理的判断。




## 要求
1. 需要使用代码仓探索的常用工具，SKILL应提前申请权限。
2. 本skill由LLM review代码的方式完成，SKILL提示词是本工具开发的重点。需要标准的提示词，创建任务列表等方式严格限制LLM的行为，避免发生任务遗忘。同时，要注意不同间接调用分析过程的相互影响。
3. 整个skill的提示词都用英文。



